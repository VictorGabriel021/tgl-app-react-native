import { KeyboardTypeOptions } from "react-native";

export type Props = {
  label: any;
  name: any;
  error: any;
  errorMessage: any;
  control: any;
  rules?: any;
  secureTextEntry?: boolean;
  keyboardType?: KeyboardTypeOptions;
};
